version: '3.1'

services:
  webserv:
    build: .
    image: webserv:ubuntu
    container_name: webserv
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"
    volumes:
      - ../../:/Webserv